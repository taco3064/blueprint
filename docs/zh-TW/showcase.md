# 導入成果展示

以同一個主題 —— 一個極小的 top-stories 閱讀器 —— 在每一種支援的技術堆疊上導入。每個範例都用到相同的組成：一個 **page**、組裝它的 **container**、葉節點 **component**、**context** provider、負責狀態的 **hook / composable**，以及取資料的 **service / lib**。比較 blueprint 如何在各堆疊塑造同一個 app。

以下每份結構，都在每次部署時以真實 CLI 對已提交的範例專案重新生成 —— `blueprint inspect` 必須通過該區塊才會建置，因此你看到的永遠是當前、真實的產出。

<!--@include: ../.generated/showcase/sections.md-->
