---
'@kekkai/blueprint': patch
---

Describe `blueprint inspect` as scanning the configured source root, with `src/` as the
default, and export `DoctorVerdict` so consumers can name the public doctor result type.
