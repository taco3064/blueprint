---
'@kekkai/blueprint': major
---

Reserve an optional module-first `app` module for framework-independent router composition. Nested `app` source now uses container semantics instead of repeating the shared layer contract.
