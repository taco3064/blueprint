---
'@kekkai/blueprint': patch
---

Keep Codex-only authoring and topology transformations free of Claude-specific command files. The neutral authoring playbook now follows the selected agent or configured `emit.agents` policy across fresh, single-application, and repository-wide flows.
