---
"@kekkai/blueprint": patch
---

Make adoption evidence fail closed: preserve ancestor ESLint policies for nested applications, scope emitted rules to the application, reject nested folder-unit relative escapes and conflicting alias identities, and report lint wiring and alias consumers independently. Unsupported ESLint impact and failed import parsing now return explicit unavailable or failed results instead of implying complete verification.

Record and verify layer-first to module-first transformation obligations across source and configuration edits. Completion requires reserved app routing, consumed container seeds, valid source-to-destination decisions, and a verified final architecture; ordinary hand-authored module-first adoption remains separate.

Repair packed public declarations and verify external npm and Yarn 1 consumers. Live affected replay and release convergence remain separate exact-candidate requirements.
