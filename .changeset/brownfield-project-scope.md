---
'@kekkai/blueprint': patch
---

Discover root-level TypeScript applications during brownfield survey, resolve aliases that
target individual architecture layers, and use a nested application's own TypeScript and
Vite configuration when `architecture.sourceRoot` scopes Blueprint below the repository root.
