---
'@kekkai/blueprint': major
---

Require an explicit topology for first adoption, inherit one topology across repository applications, and coordinate configured topology changes repository-wide. Source-tree survey evidence no longer classifies unmanaged projects, and presets are limited to layer-first adoption.
