---
name: field-validation
description: Select live Blueprint field targets and choose a narrow repair replay or a full private pre-release matrix. Field execution is owner-operated and is not a GitHub Actions release gate.
---

# Choose field-validation scope

Read and follow [the shared autonomous delivery policy](../../docs/autonomous-delivery.md) and [field triage](../../docs/field-triage.md).

This skill owns only Field target-role judgment and replay scope. Field is private owner-operated pre-release validation. It does not create release authority, GitHub commit statuses, or Actions gates.

## Required target roles

Preserve these established roles unless the owner changes the validation contract:

| Target | Role |
|---|---|
| Vue Pure Admin | Real-world conventional `src/` adopter control |
| React DDD Feature Folder | Real-world outer-axis control with an intentional inner-architecture model boundary |
| RealWorld React FSD | Real-world feature-sliced layer and alias control |
| Vue Vben Admin `apps/web-antd` | Real-world nested workspace and application-scope control |
| sky-1945 | Layer-first → module-first → layer-first transformation control |
| Repository-owned new-project starters | Greenfield controls when the owner includes them |

Real-world adopters test safe understanding and adoption of their existing architecture; they are not topology-conversion controls. sky-1945 owns the bidirectional transformation role. Do not silently substitute one role for another or treat adopter debt and supported model boundaries as Blueprint defects.

## Choose replay scope

Use **affected replay** to confirm a known finding after repair when the changed behavior and blast radius justify a narrow target set.

Use a **full private matrix** when the owner wants broad release confidence, shared Agent-facing/adoption behavior changed broadly, or the blast radius cannot be narrowed confidently.

Return the selected scope, target roles, and short blast-radius rationale. Do not add GitHub status, release-gate, candidate-recording, or Actions mechanics.